<template name="switch">
  	<view 
		id="{{id}}"
		class="switch {{isOn ? 'toggle-on' : 'toggle-off'}}" 
	  	style="background-color:{{isOn ? onTintColor || 'rgb(16,142,233)' : '#ddd'}}"
		bindtap="tapSwitch"
	>
	</view>
</template>
